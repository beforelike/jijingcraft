// Root build.gradle — Ponderer multi-platform (Lotus architecture)
// Subprojects: Common, Forge, Fabric

tasks.withType(Test).configureEach {
    it.enabled = false
}

subprojects {
    apply plugin: "java-library"
    apply plugin: "maven-publish"
    apply plugin: "idea"

    version = mod_version
    group = mod_group_id

    java.toolchain.languageVersion = JavaLanguageVersion.of(17)

    tasks.withType(JavaCompile).configureEach {
        options.encoding = 'UTF-8'
        outputs.cacheIf { false }
    }

    repositories {
        mavenLocal()
        mavenCentral()
        maven { url = "https://repo.spongepowered.org/repository/maven-public/" }
        maven { url = "https://raw.githubusercontent.com/Fuzss/modresources/main/maven/" }
        maven { url = "https://maven.createmod.net" }
        maven { url = "https://maven.blamejared.com" }
        maven { url = "https://maven.terraformersmc.com/releases/" }
        maven { url = "https://mvn.devos.one/snapshots/" }
        maven { url = "https://mvn.devos.one/releases/" }
        maven { url = "https://maven.ladysnake.org/releases" }
        maven { url = "https://maven.jamieswhiteshirt.com/libs-release/" }
        maven {
            name = "Modrinth"
            url = "https://api.modrinth.com/maven"
            content {
                includeGroup "maven.modrinth"
            }
        }
    }

    // Token expansion for mod metadata files
    processResources {
        def expandProps = [
            "version"                   : mod_version,
            "group"                     : mod_group_id,
            "minecraft_version"         : minecraft_version,
            "forge_version"             : forge_version,
            "forge_version_range"       : forge_version_range,
            "minecraft_version_range"   : minecraft_version_range,
            "fabric_version"            : fabric_version,
            "fabric_loader_version"     : fabric_loader_version,
            "loader_version_range"      : loader_version_range,
            "mod_name"                  : mod_name,
            "mod_id"                    : mod_id,
            "mod_authors"               : mod_authors,
            "mod_description"           : mod_description,
            "mod_license"               : mod_license,
            "mod_version"               : mod_version,
            "jei_version"               : jei_version
        ]

        filesMatching(["pack.mcmeta", "fabric.mod.json", "META-INF/mods.toml", "*.mixins.json"]) {
            expand expandProps
        }
        inputs.properties(expandProps)
    }

    idea {
        module {
            downloadSources = true
            downloadJavadoc = true
        }
    }
}

// ─── 自动化测试启动 tasks ───────────────────────────────────────────────────
tasks.register('testFabric', Exec) {
    group = 'test'
    description = 'Build Fabric and launch test.py for Fabric instance'
    dependsOn ':Fabric:build'
    commandLine 'python', 'test.py', 'fabric', '--skip-build'
}
tasks.register('testForge', Exec) {
    group = 'test'
    description = 'Build Forge and launch test.py for Forge/NeoForge instance'
    dependsOn ':Forge:build'
    commandLine 'python', 'test.py', 'forge', '--skip-build'
}
tasks.register('testBoth', Exec) {
    group = 'test'
    description = 'Build Fabric and Forge then launch both instances'
    dependsOn ':Fabric:build', ':Forge:build'
    commandLine 'python', 'test.py', 'both', '--skip-build'
}
